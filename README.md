# Version-Control-System
This project implements a simplified version control system (PES-VCS) inspired by Git. It supports file tracking, staging, commit creation, and history traversal using content- addressable storage.
